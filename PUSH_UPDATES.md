# Push All API Updates to GitHub

Run these commands in PowerShell or Git Bash:

```bash
cd c:\Users\bkarunya\Downloads\Blog-Website

git add .

git commit -m "Fix: Update all pages to use centralized API configuration"

git push
```

This will push all the updated files and trigger Vercel to rebuild your app with full functionality!
